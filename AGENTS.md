# 项目协作说明

## 版本管理流程

当我对你说“执行版本管理流程”“做版本记录”“提交当前版本”“版本管理”“进行版本管理”这类短句时，你必须按以下流程执行：

### 版本管理执行步骤

1. 先运行 `git status`，检查当前项目是否有未提交改动。
2. 如果没有改动，告诉我当前没有需要提交的内容，不要强行创建 commit。
3. 如果有改动，先总结本次修改涉及的文件和主要内容。
4. 读取当前 `VERSION.md` 文件内容。
5. 不要覆盖 `VERSION.md` 原有内容。
6. 在 `VERSION.md` 文档最上方追加本次最新版本记录，旧版本记录整体往下保留。
7. 如果 `VERSION.md` 不存在，则新建 `VERSION.md`。
8. 本次版本记录格式如下：

```markdown
# 版本记录

## vX.X.X - YYYY-MM-DD HH:mm

### 更新摘要
用 2-4 句话总结本次更新的核心变化。

### 主要改动
- 改动 1
- 改动 2
- 改动 3

### 涉及文件
- 文件路径 1：说明修改内容
- 文件路径 2：说明修改内容
- 文件路径 3：说明修改内容

### 验证情况
- 是否已运行构建：
- 是否已本地预览：
- 是否存在待处理问题：

### Git 信息
- Commit ID：
- Commit message：

---
```

9. 版本号请根据当前 `VERSION.md` 中已有记录自动递增：
   - 小修小改：递增最后一位，例如 `v1.0.1` -> `v1.0.2`
   - 功能更新：递增中间位，例如 `v1.0.2` -> `v1.1.0`
   - 重大重构：递增第一位，例如 `v1.9.0` -> `v2.0.0`
10. 更新 `VERSION.md` 后，执行：
    - `git status`
    - `git add .`
    - `git commit -m "版本更新：简要描述本次更新"`
11. 不要执行 `git push`。
12. 最后用中文告诉我：
    - 本次版本号
    - 修改了哪些文件
    - `VERSION.md` 是否已成功追加记录
    - 最新 commit ID
    - 下一步是否需要我手动部署或同步到云端
